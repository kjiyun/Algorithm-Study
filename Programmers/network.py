#네트워크

import sys
input = sys.stdin.readline

def solution(n, computers):
    answer = 0
    return answer

n = int(input())  # 컴퓨터의 개수
graph = [[0]*n]

for _ in range(n):
    graph.append(list(map(int, input().split())))

